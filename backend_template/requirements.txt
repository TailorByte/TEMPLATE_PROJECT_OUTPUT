# Django Core
Django~=4.2 # Or your preferred LTS version, e.g., 3.2, 5.0
djangorestframework~=3.14
python-dotenv~=1.0
psycopg2-binary~=2.9 # For PostgreSQL
gunicorn~=21.2 # For production deployment

# API Documentation
drf-spectacular~=0.26

# Optional, but common:
# djangorestframework-simplejwt~=5.3 # For JWT authentication (will be added if JWT setup is included)
# django-cors-headers~=4.3 # For Cross-Origin Resource Sharing
# celery~=5.3 # For asynchronous tasks
# redis~=5.0 # For Celery broker/backend or caching

# Linters & Formatters (often managed as dev dependencies or via pre-commit)
# flake8
# black
# isort